framework:
    validation:
        email_validation_mode: html5
        mapping:
            paths:
                - '%kernel.project_dir%/config/resources/validation/Core'

when@test:
    framework:
        validation:
            not_compromised_password: false
